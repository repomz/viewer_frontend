name: viewer-frontend

services:
  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/repomz/viewer_frontend:0.2.37}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${IMAGE_VERSION:-0.2.5}
        VCS_REF: ${VCS_REF:-unknown}
    environment:
      PUBLIC_DOMAIN: ${PUBLIC_DOMAIN:-angio.su}
      BACKEND_URL: ${BACKEND_URL:-https://angio.su/api}
      PACS_URL: ${PACS_URL:-https://angio.su}
      PACS_AUTHORIZATION: ${PACS_AUTHORIZATION:-Basic bWFwZHI6Y2hhbmdlc3Ryb25ncGFzc3dvcmQ=}
    ports:
      - "${FRONTEND_PORT:-5173}:8080"
    restart: unless-stopped
    init: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
